apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx
  name: nginx-2
  namespace: payment-api
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always