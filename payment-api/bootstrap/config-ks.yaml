apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: tenant-config
  namespace: payment-api
spec:
  prune: true
  serviceAccountName: flux-payment-api
  interval: 5m0s
  path: /payment-api/config/${environment}
  sourceRef:
    kind: GitRepository
    name: source